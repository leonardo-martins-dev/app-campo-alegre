# Movido para monorepo

Este projeto agora vive em **[../campo-alegre](../campo-alegre)**:

- `mobile/` — app Expo
- `web/` — painel admin
- `supabase/` — backend SQL

Use o repositório Git em `campo-alegre/`.
