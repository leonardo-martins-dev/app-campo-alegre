# Monorepo Campo Alegre

# Dependências na raiz (legado / instalados por engano)
node_modules/

# App mobile (Expo)
mobile/node_modules/
mobile/.expo/
mobile/dist/
mobile/web-build/
mobile/ios/
mobile/android/
mobile/.env
mobile/.env*.local

# Painel web (Next.js)
web/node_modules/
web/.next/
web/.env.local

# Supabase (secrets locais)
supabase/.env

# OS / IDE
.DS_Store
*.pem

# Projetos legados (fora do Campo Alegre Hub)
jotter-logix/
remix-of-campo-alegre-linker/
